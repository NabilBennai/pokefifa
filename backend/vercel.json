{
  "version": 2,
  "installCommand": "npm ci",
  "buildCommand": "npm run prisma:generate && npm run build",
  "outputDirectory": "public",
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.ts"
    }
  ]
}
